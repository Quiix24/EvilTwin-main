╔══════════════════════════════════════════════════════════════════════════════════════════════════════╗
║              EVILTWIN — INTELLIGENT SSH CYBER DECEPTION PLATFORM  (Full Architecture)               ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  PHASE 1 — CONNECTION ARRIVES                                                                      │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

                            ┌──────────────────────────────────┐
                            │       SSH Client / User          │
                            │  (attacker OR legitimate user)   │
                            └────────────────┬─────────────────┘
                                             │  TCP port 22
                                             ▼

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  PHASE 2 — FEATURE EXTRACTION                                                                      │
│  file: gateway/gateway.py  ──  GatewayServer + SessionSignals                                      │
│  Gateway terminates the SSH handshake. No backend connected yet. Silent observation only.          │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────────────────────────────────────┐
                    │             SSH GATEWAY  (port 22)                     │
                    │  GatewayServer.connection_made()                        │
                    │  GatewayServer.begin_auth()                             │
                    │  GatewayServer.password_auth_requested()                │
                    │  GatewayServer.public_key_auth_requested()              │
                    │                                                         │
                    │  Signals collected into SessionSignals:                 │
                    │  ├── src_ip, src_port                                   │
                    │  ├── client_version       (e.g. OpenSSH_8.4p1)         │
                    │  ├── kex_algorithms_hash   (MD5 fingerprint of KEX)     │
                    │  ├── time_to_first_auth    (connect → first auth gap)   │
                    │  ├── auth_attempts_count   (how many tries made)        │
                    │  ├── auth_methods_used     (password / publickey)       │
                    │  ├── usernames_tried       (root? admin? real name?)    │
                    │  ├── auth_attempt_interval (speed between attempts)     │
                    │  ├── shell_requested / exec_command                     │
                    │  └── is_interactive                                     │
                    │                                                         │
                    │  sig.ready fires when:                                  │
                    │  ├── publickey auth attempted   (instant strong signal) │
                    │  ├── >= 3 password attempts     (spray pattern)         │
                    │  └── shell / exec requested     (always triggers it)    │
                    │                                                         │
                    │  Max collection window: AUTH_COLLECT_WINDOW_S (5s)      │
                    └──────────────────────────┬──────────────────────────────┘
                                               │
                                               │  SessionSignals.to_payload()
                                               │  HTTP POST /score/initial
                                               │  timeout: 2s  │  safe-fail → honeypot
                                               ▼

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  PHASE 3 — AI CLASSIFICATION  (no backend connected yet — classify first)                          │
│  file: backend/routers/scoring.py  ──  score_initial()                                             │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────────────────────────────────────────────────────────┐
        │                   FAST AI CLASSIFIER   /score/initial                   │
        │                                                                          │
        │  STEP A — Heuristic Engine  (primary · stateless · zero I/O)            │
        │  _heuristic()  evaluated top-to-bottom, first match wins:               │
        │                                                                          │
        │  Rule 1  IP inside WHITELIST_CIDRS?           ──▶  REAL     conf 1.00   │
        │  Rule 2  publickey auth + clean username?     ──▶  REAL     conf 0.92   │
        │  Rule 3  auth_attempts_count >= 4?            ──▶  HONEYPOT conf 0.97   │
        │  Rule 4  suspicious username tried?           ──▶  HONEYPOT conf 0.90   │
        │  Rule 5  auth_attempt_interval < 0.3s?        ──▶  HONEYPOT conf 0.90   │
        │  Rule 6  time_to_first_auth < 0.15s  (bot)?  ──▶  HONEYPOT conf 0.82   │
        │  Rule 7  suspicious commands in payload?      ──▶  HONEYPOT conf 0.95   │
        │  Rule 8  clean non-interactive exec command?  ──▶  REAL     conf 0.72   │
        │  Rule 9  default safe fallback                ──▶  HONEYPOT conf 0.60   │
        │                                                                          │
        │  STEP B — ML Model Refinement  (secondary · confidence adjust only)     │
        │  _ml_presession_level() → threat_scorer.pipeline.predict()              │
        │  Available pre-session features: credential_spray, hour_of_day,         │
        │  is_weekend  (post-session features = 0, no commands yet)               │
        │                                                                          │
        │  ML agrees with heuristic?           ──▶  confidence += 0.04            │
        │  ML disagrees + heuristic conf<0.85? ──▶  confidence -= 0.08            │
        │  Heuristic conf >= 0.85?             ──▶  ignore ML, trust heuristic    │
        │                                                                          │
        │  Returns:  { decision: "real" | "honeypot",  confidence,  reason }      │
        └────────────────────────────────────┬─────────────────────────────────────┘
                                             │
                                             │  decision returned to gateway
                                             ▼

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  PHASE 4 — SDN ENFORCEMENT  (the actual network redirector)                                        │
│  file: sdn/  ──  Ryu Controller  (port 8080)                                                       │
│  The gateway hands the AI decision to Ryu. Ryu installs the flow rule. Traffic is redirected.      │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────────────────────────────────────┐
                    │           SDN CONTROLLER — Ryu  (port 8080)            │
                    │                                                         │
                    │  Receives AI decision from gateway via REST             │
                    │                                                         │
                    │  decision = "real"                                      │
                    │  ├── POST /whitelist { ip, idle_timeout: 3600 }         │
                    │  ├── Installs OVS bypass flow rule for this src_ip      │
                    │  └── Network forwards packets ──▶ REAL SSH SERVER       │
                    │                                                         │
                    │  decision = "honeypot"                                  │
                    │  ├── No whitelist entry                                 │
                    │  ├── Default OVS rule stays active                      │
                    │  └── Network forwards packets ──▶ DECEPTION NETWORK     │
                    │                                                         │
                    │  SDN_AUTO_RELEASE = true  (optional post-session)       │
                    │  └── If ML scores level=0 + score=0.0 after session     │
                    │      auto-whitelist the IP (release benign user)        │
                    └──────────────┬──────────────────────────┬───────────────┘
                                   │                          │
                      decision = "real"          decision = "honeypot"
                                   │                          │
                                   ▼                          ▼

┌──────────────────────────────────────────┐  ┌──────────────────────────────────────────────────────┐
│  PHASE 5A — LEGITIMATE USER              │  │  PHASE 5B — ATTACKER PATH                            │
│  file: gateway/gateway.py               │  │  file: gateway/gateway.py                            │
│  _proxy_to_real()                        │  │  _proxy_to_honeypot()                                │
│                                          │  │                                                      │
│  ├── Connect to REAL_SSH_HOST:PORT       │  │  Traffic enters DECEPTION NETWORK                    │
│  ├── Auth with captured credentials      │  │  (deception-net — isolated Docker network)           │
│  ├── Bidirectional transparent proxy     │  │                                                      │
│  ├── User never knows they were          │  │  ╔════════════════════════════════════════════════╗  │
│  │   observed by the gateway             │  │  ║  🐋 DOCKER CONTAINER — COWRIE                 ║  │
│  │                                       │  │  ║  Image: cowrie/cowrie:latest                  ║  │
│  └── Session ends cleanly               │  │  ║  User: cowrie (non-root, isolated)             ║  │
│      No DB write · No score              │  │  ║  Network: deception-net + eviltwin-net         ║  │
│      No alert · No trace                 │  │  ║  Port: 2222 (mapped) / 22 (internal)           ║  │
│      Legitimate users are invisible      │  │  ║  Volume: cowrie-logs (shared)                  ║  │
│      to the deception system             │  │  ║                                                ║  │
└──────────────────────────────────────────┘  │  ║  Simulates: OpenSSH_7.4 "prod-web-01"         ║  │
                                              │  ║  Attacker sees a realistic fake Linux shell    ║  │
                                              │  ║                                                ║  │
                                              │  ║  Captures everything attacker does:            ║  │
                                              │  ║  ├── Every command typed                       ║  │
                                              │  ║  ├── All credentials attempted                 ║  │
                                              │  ║  ├── File downloads (malware binaries)         ║  │
                                              │  ║  ├── Lateral movement attempts                 ║  │
                                              │  ║  └── Full session transcript                   ║  │
                                              │  ║                                                ║  │
                                              │  ║  Emits: cowrie.json  (continuous log stream)   ║  │
                                              │  ║  Written to shared Docker volume: cowrie-logs  ║  │
                                              │  ╚════════════════════════════════════════════════╝  │
                                              │                                                      │
                                              │  ╔════════════════════════════════════════════════╗  │
                                              │  ║  🐋 DOCKER CONTAINER — DIONAEA               ║  │
                                              │  ║  Image: dinotools/dionaea:latest              ║  │
                                              │  ║  User: nobody (non-root, isolated)            ║  │
                                              │  ║  Network: deception-net + eviltwin-net        ║  │
                                              │  ║  Ports: 21, 80, 445, 1433  (exposed)         ║  │
                                              │  ║  Volume: dionaea-logs, dionaea-captures       ║  │
                                              │  ║                                               ║  │
                                              │  ║  Multi-protocol deception layer:             ║  │
                                              │  ║  ├── port 21   FTP  → file transfer exploits ║  │
                                              │  ║  ├── port 80   HTTP → web exploits/shellcode ║  │
                                              │  ║  ├── port 445  SMB  → EternalBlue/ransomware ║  │
                                              │  ║  ├── port 1433 MSSQL→ database attacks       ║  │
                                              │  ║  └── port 5060 SIP  → VoIP scanning          ║  │
                                              │  ║                                               ║  │
                                              │  ║  Captures:                                    ║  │
                                              │  ║  ├── Malware binaries → /binaries volume      ║  │
                                              │  ║  ├── Exploit payloads + shellcode             ║  │
                                              │  ║  ├── Protocol interaction logs               ║  │
                                              │  ║  └── dionaea.json log stream                 ║  │
                                              │  ╚════════════════════════════════════════════════╝  │
                                              └──────────────────────────────────┬───────────────────┘
                                                                                 │
                                              ┌──────────────────────────────────▼───────────────────┐
                                              │  Both honeypots send logs through TWO parallel paths  │
                                              └──────────────────────────────────────────────────────┘
                                                          │                        │
                               ┌────────────────────────────┐    ┌────────────────────────────────────┐
                               │  PATH A — Gateway report   │    │  PATH B — Raw log forwarding       │
                               │  after SSH session ends     │    │  event-by-event as it happens      │
                               │                            │    │                                    │
                               │  POST /score/gateway-session│    │  cowrie.json volume                │
                               │  payload: GatewaySessionReport│  │      │                            │
                               │  timeout: 10s              │    │      ▼                            │
                               │  includes:                 │    │  🐋 DOCKER CONTAINER               │
                               │  ├── early_commands[:50]   │    │  Splunk Universal Forwarder        │
                               │  ├── has_suspicious_cmds   │    │  inputs.conf monitors:             │
                               │  ├── session_duration_s    │    │  /cowrie/var/log/cowrie/cowrie.json │
                               │  └── all auth signals      │    │  index = eviltwin                  │
                               └──────────────┬─────────────┘    │  sourcetype = cowrie:json          │
                                              │                  │      │                            │
                                              │                  │  outputs.conf forwards to:         │
                                              │                  │  splunk:9997  (TCP indexer)        │
                                              │                  └──────────────┬─────────────────────┘
                                              │                                 │
                                              ▼                                 ▼

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  PHASE 6 — POST-SESSION AI THREAT SCORING + PERSISTENCE                                            │
│  files: backend/routers/scoring.py  ──  report_gateway_session()                                   │
│         backend/routers/ingest.py   ──  ingest_log()                                               │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────────────────────────────────────┐
                    │  STEP 1 — ATTACKER PROFILE  (attacker_profiles table)  │
                    │  ├── Create or update record by src_ip                  │
                    │  ├── Increment total_sessions counter                   │
                    │  ├── VPN / Proxy / Tor detection                        │
                    │  │   services/vpn_detection.py  ::  VPNDetector         │
                    │  └── Geolocation enrichment:                            │
                    │      country · city · ISP · latitude · longitude        │
                    └──────────────────────────┬──────────────────────────────┘
                                               │
                                               ▼
                    ┌─────────────────────────────────────────────────────────┐
                    │  STEP 2 — SESSION LOG  (session_logs table)            │
                    │  ├── session_id      UUID5 deterministic               │
                    │  ├── honeypot        "cowrie"  or  "dionaea"           │
                    │  ├── protocol        "ssh" / "http" / "smb" / "ftp"   │
                    │  ├── start_time, end_time                              │
                    │  ├── commands        [{timestamp, command, output}]    │
                    │  ├── credentials_tried  [{username, password, ok}]     │
                    │  ├── malware_hashes   [sha256 ...]                     │
                    │  └── raw_log          full event JSON  (JSONB)         │
                    └──────────────────────────┬──────────────────────────────┘
                                               │
                                               ▼
                    ┌─────────────────────────────────────────────────────────┐
                    │  STEP 3 — FULL ML THREAT SCORING                       │
                    │  ThreatScorer.score(session, profile)                   │
                    │  file: backend/services/threat_scorer.py               │
                    │                                                         │
                    │  16 features extracted  (ai/feature_extractor.py):     │
                    │  ├── cmd_count              total commands typed        │
                    │  ├── unique_cmd_ratio        diversity of commands      │
                    │  ├── download_attempt        wget / curl / tftp seen?   │
                    │  ├── priv_escalation_attempt sudo / chmod / passwd?     │
                    │  ├── credential_spray        failed_logins > 3          │
                    │  ├── session_duration_s      how long they stayed       │
                    │  ├── commands_per_minute     speed of attack            │
                    │  ├── hour_of_day             attack timing pattern      │
                    │  ├── is_weekend              weekend bot activity        │
                    │  ├── vpn_detected            hiding behind VPN?         │
                    │  ├── known_bad_ip            AbuseIPDB reputation hit   │
                    │  ├── recon_commands          id/whoami/uname/ifconfig   │
                    │  ├── persistence_attempt     crontab/.bashrc/systemd    │
                    │  ├── data_exfil_attempt      scp/base64/cat passwd      │
                    │  ├── malware_dropped         binaries downloaded         │
                    │  └── multi_protocol          seen across multiple ports  │
                    │                                                         │
                    │  RandomForest pipeline  (ai/model.pkl):                │
                    │  ├── predict_proba() → threat_score  (0.0 – 1.0)      │
                    │  └── predict()       → threat_level  (0 – 4)           │
                    │                                                         │
                    │  0=benign  1=low  2=medium  3=high  4=critical          │
                    │                                                         │
                    │  TTL cache per IP  (SCORE_CACHE_TTL seconds)           │
                    └──────────────────────────┬──────────────────────────────┘
                                               │
                               ┌───────────────▼───────────────┐
                               │    threat_level >= 3?         │
                               │    HIGH  or  CRITICAL         │
                               └───────┬───────────────┬───────┘
                                      YES               NO
                                       │                │
                                       ▼                ▼
                    ┌──────────────────────────┐   ┌──────────────────────────────┐
                    │  STEP 4 — CREATE ALERT   │   │  Update profile threat score │
                    │  (alerts table)          │   │  Commit session to DB        │
                    │  ├── session_id          │   │  Done — no alert needed      │
                    │  ├── attacker_ip         │   └──────────────────────────────┘
                    │  ├── threat_level (3/4)  │
                    │  └── message             │
                    └────────────┬─────────────┘
                                 │
                    ┌────────────┴─────────────────────────────────────────────┐
                    │                                                          │
                    ▼                                                          ▼
     ┌──────────────────────────────┐               ┌────────────────────────────────────────┐
     │  AlertManager.broadcast()    │               │  SplunkForwarder.send_event()          │
     │  file: services/             │               │  file: services/splunk_forwarder.py    │
     │  alert_manager.py            │               │                                        │
     │                              │               │  HTTP POST to SPLUNK_HEC_URL           │
     │  In-memory async queue       │               │  Header: Authorization: Splunk <token> │
     │  one queue per connected     │               │  Payload:                              │
     │  dashboard WebSocket client  │               │  ├── event: { alert + raw_log }        │
     │                              │               │  ├── source: "eviltwin-ingest"         │
     │                              │               │  │        or "eviltwin-gateway"        │
     │                              │               │  ├── sourcetype: "cowrie:json"         │
     │                              │               │  └── index: "eviltwin"                 │
     └──────────────────────────────┘               └────────────────────────────────────────┘
                    │                                                          │
                    ▼                                                          ▼

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  PHASE 7 — TWO ANALYST LAYERS  (Security Operations Center)                                        │
│  Analysts can use EITHER or BOTH simultaneously                                                    │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

  ╔═══════════════════════════════════════════════════════════════╗
  ║  LAYER A — EVILTWIN DASHBOARD  (React · port 3000)           ║
  ║  For operational SOC — real-time alerts + session drill-down  ║
  ╚═══════════════════════════════════════════════════════════════╝

  ┌───────────────────┐ ┌───────────────────┐ ┌──────────────────┐ ┌────────────────────────┐
  │   LIVE ALERTS     │ │    SESSIONS       │ │   STATISTICS     │ │     AI ANALYST         │
  │                   │ │                   │ │                  │ │                        │
  │ WebSocket push    │ │ GET /sessions     │ │ GET /stats       │ │ POST /ai/analyze       │
  │ /ws/alerts        │ │ Filter by:        │ │ ├ sessions/24h   │ │                        │
  │ JWT-authenticated │ │ ├ honeypot        │ │ ├ unique IPs/24h │ │ LLM deep analysis:     │
  │                   │ │ ├ threat_level    │ │ ├ critical/24h   │ │ ├── TTPs (MITRE ATT&CK)│
  │ Alert card fires  │ │ ├ date range      │ │ ├ top 10 cmds    │ │ ├── IOC indicators     │
  │ when level >= 3   │ │ ├ src_ip          │ │ ├ attacks/hour   │ │ ├── risk assessment    │
  │                   │ │ └ page / size     │ │ └ level distrib  │ │ └── recommendations    │
  │ Shows:            │ │                   │ │                  │ │                        │
  │ ├ attacker IP     │ │ Click → detail:   │ │ GET /stats/      │ │ POST /ai/chat          │
  │ ├ threat level    │ │ ├ commands list   │ │ timeline         │ │ Conversational threat  │
  │ ├ message         │ │ ├ credentials     │ │                  │ │ intel with session ctx │
  │ └ timestamp       │ │ ├ malware hashes  │ │ GET /stats/      │ │                        │
  │                   │ │ ├ country/ISP/VPN │ │ top-attackers    │ │ (needs LLM_API_KEY)    │
  │ Acknowledge btn   │ │ └ threat score    │ │                  │ │                        │
  │ (SOC workflow)    │ │                   │ │                  │ │                        │
  └───────────────────┘ └───────────────────┘ └──────────────────┘ └────────────────────────┘

  ╔═══════════════════════════════════════════════════════════════╗
  ║  LAYER B — SPLUNK DASHBOARD  (Technical SOC / SIEM team)     ║
  ║  For deep log forensics, hunting, compliance, correlation     ║
  ╚═══════════════════════════════════════════════════════════════╝

  ┌───────────────────────────────────────────────────────────────────────────────┐
  │  Splunk index: eviltwin   sourcetype: cowrie:json                             │
  │                                                                               │
  │  Data arrives from TWO sources:                                               │
  │  ├── Universal Forwarder → raw cowrie.json lines → port 9997 indexer          │
  │  └── Backend HEC POST   → enriched alerts with threat_score + threat_level    │
  │                                                                               │
  │  eviltwin_overview.xml dashboard panels:                                      │
  │                                                                               │
  │  ┌─────────────────────────────────────────────────────────────────────────┐  │
  │  │  Panel 1 — Attack Count Over Time                                       │  │
  │  │  index=eviltwin sourcetype=cowrie:json | timechart span=1h count        │  │
  │  │  → Area chart, 24h window                                               │  │
  │  └─────────────────────────────────────────────────────────────────────────┘  │
  │  ┌─────────────────────────────────────────────────────────────────────────┐  │
  │  │  Panel 2 — Top 10 Attacker Commands                                     │  │
  │  │  eventid="cowrie.command.input" | top limit=10 input                    │  │
  │  │  → Table of most-used attacker commands                                 │  │
  │  └─────────────────────────────────────────────────────────────────────────┘  │
  │  ┌─────────────────────────────────────────────────────────────────────────┐  │
  │  │  Panel 3 — Threat Level Distribution                                    │  │
  │  │  eval threat=case(0=Unknown,1=Low,2=Medium,3=High,4=Critical)           │  │
  │  │  → Pie chart breakdown                                                  │  │
  │  └─────────────────────────────────────────────────────────────────────────┘  │
  │  ┌─────────────────────────────────────────────────────────────────────────┐  │
  │  │  Panel 4 — Critical Alerts Table                                        │  │
  │  │  threat_level>=3 | table _time src_ip country input eventid | sort -_time│  │
  │  │  → Live table of high + critical events                                 │  │
  │  └─────────────────────────────────────────────────────────────────────────┘  │
  └───────────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  INFRASTRUCTURE — DOCKER + NETWORK TOPOLOGY                                                        │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

  🐋 Docker Containers:
  ├── eviltwin-gateway        gateway/Dockerfile          port 22
  ├── eviltwin-backend        backend/Dockerfile          port 8000
  ├── eviltwin-frontend       frontend/Dockerfile         port 3000
  ├── eviltwin-postgres       postgres:16-alpine          port 5432
  ├── eviltwin-ryu            sdn/Dockerfile              port 6633 + 8080
  ├── eviltwin-cowrie         honeypots/cowrie/Dockerfile port 2222  ← deception-net isolated
  ├── eviltwin-dionaea        honeypots/dionaea/Dockerfile ports 21/80/445/1433 ← deception-net
  └── eviltwin-splunk-fwd     splunk/universalforwarder   (reads cowrie-logs volume)

  Docker Networks:
  ├── eviltwin-net   (bridge) — all services communicate here
  └── deception-net  (bridge · internal=true) — Cowrie + Dionaea ISOLATED here
                     attackers cannot reach real network from inside deception-net

  Shared Volumes:
  ├── cowrie-logs     ← written by Cowrie · read by Splunk Forwarder + Backend
  ├── cowrie-downloads← malware binaries captured by Cowrie
  ├── dionaea-logs    ← written by Dionaea · read by Backend
  ├── dionaea-captures← malware binaries captured by Dionaea
  └── gateway-hostkey ← SSH host key persisted across restarts

  PostgreSQL tables:
  ├── users              dashboard admin accounts
  ├── attacker_profiles  one row per attacker IP (score, level, geo, VPN)
  ├── session_logs       one row per honeypot session (commands, creds, malware)
  └── alerts             one row per level >= 3 event (with acknowledge tracking)
